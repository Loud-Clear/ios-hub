language: objective-c
osx_image: xcode8.3
cache:
  - cocoapods

before_script:
  - pod install

script:
  - fastlane scan "iOS Hub.xcworkspace" --scheme "iOS Hub"

after_success:
  - bash <(curl -s https://codecov.io/bash)
